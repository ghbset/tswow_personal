# Add directories, files, and symbolic links which should not be stored within
# the repository here.

# Helper files requiring special attention
!.gitignore
!.gitattributes

# Project files
/.cache/
/.idea/
/.vscode/

# OS specific temporary files
.DS_Store
Thumbs.db

# NodeJS
/node_modules/
/package-lock.json

# Configuration files
/build.conf

# Logs
/log.txt
package-lock.json
package.json

# Submodule / local install artifacts
cores/TrinityCore

# Modules live in tswow-install/modules — never track them from this repo
/modules/

# Local staging copies of TC source files (kept at repo root during
# Duskhaven port work — not part of the tswow build)
/Creature.cpp
/DynamicObject.cpp
/InstanceScript.cpp
/Pet.h
/SpellAuras.cpp
/SpellHistory.cpp
/TemporarySummon.cpp
/Totem.cpp

# Local tools and one-off scripts
/llm-service/
/fix-vendor-hang.sh
/worldserver_performance_indexes.sql
